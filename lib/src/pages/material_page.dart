import 'package:flutter/material.dart';
class MaterialPage extends StatelessWidget {
  const MaterialPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Material por asignatura"),
    );
  }
}