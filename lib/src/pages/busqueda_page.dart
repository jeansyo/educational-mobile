import 'package:flutter/material.dart';
class BusquedaPage extends StatelessWidget {
  const BusquedaPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("busqueda de material"),
    );
  }
}